{
  "name": "lugand-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Monorepo para o sistema de componentes Lugand UI - Vue 3 + TypeScript",
  "author": "Lugand Sistemas",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run build:lib && npm run dev --workspace=test-ui-lib",
    "dev:lib": "npm run dev --workspace=@lugand/vue-ui-lib",
    "dev:watch": "npm run build:lib && npm run dev --workspace=test-ui-lib",
    "build:lib": "npm run build:lib --workspace=@lugand/vue-ui-lib",
    "build:test": "npm run build --workspace=test-ui-lib",
    "build:all": "npm run build:lib && npm run build:test",
    "type-check": "npm run type-check --workspaces --if-present",
    "clean": "rm -rf node_modules packages/*/node_modules packages/*/dist",
    "install:all": "npm install"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "npm": ">=10.0.0"
  }
}