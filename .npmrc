# Configuração do npm para o monorepo Lugand UI

# Instalação mais rápida
prefer-offline=true

# Log level
loglevel=warn

# Salvar exact versions
save-exact=false
save-prefix=^

# Legacy peer deps (caso necessário)
# legacy-peer-deps=false
